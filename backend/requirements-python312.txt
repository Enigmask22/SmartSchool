# Python 3.12 Compatible Requirements for Render
# Sử dụng pre-built wheels từ PyPI

# ============================================
# PyTorch với CUDA 12.4 Support
# ============================================
# Thêm index URL cho PyTorch CUDA wheels
--extra-index-url https://download.pytorch.org/whl/cu124

# Core ML & CV packages với Python 3.13 support
opencv-python-headless>=4.9.0
insightface>=0.7.3
onnxruntime>=1.18.0
onnx>=1.16.0
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.1.0

# Cryptography packages
python-jose[cryptography]>=3.3.0
bcrypt>=4.0,<5.0  # Lock
cryptography>=41.0.0
PyJWT>=2.8.0

# Image processing
Pillow>=10.2.0

# FastAPI ecosystem
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.7
email-validator>=2.1.1

# Database & external services
supabase>=2.4.0
requests>=2.31.0
aiofiles>=23.2.1

# Utilities
python-dotenv>=1.0.1
python-dateutil>=2.8.2
typing-extensions>=4.9.0
websockets>=12.0
google-generativeai>=0.8.3
schedule>=1.2.2
setuptools>=69.0.0
wheel>=0.42.0

#XLSX
xlsxwriter>=3.1.0

# ============================================
# OCR for Handwriting Grade Sheet
# ============================================
# Option 1: Gemini Vision API (already included in google-generativeai)
# Option 2: Qwen2.5-VL-3B (local model - RECOMMENDED for GPU users)

# PyTorch with CUDA 12.4 support (GPU acceleration)
# Đã config --extra-index-url ở trên → pip sẽ tự động cài CUDA version
# 
# ✅ Install với file này:
#   pip install -r requirements-python313.txt
#
# 🔍 Verify CUDA sau khi cài:
#   python -c "import torch; print('CUDA:', torch.cuda.is_available())"
#   Expected output: CUDA: True
#
# 💡 Lưu ý:
#   - File này dùng CUDA 12.4 (phù hợp với RTX 4060)
#   - Nếu dùng CUDA 11.8: đổi cu124 → cu118 ở --extra-index-url
#   - Nếu chỉ cần CPU: xóa dòng --extra-index-url
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
# Transformers & Vision model dependencies
transformers>=4.37.0
accelerate>=0.25.0
sentencepiece>=0.1.99
einops>=0.7.0
timm>=0.9.0

# Qwen2.5-VL dependencies (for Qwen2.5-VL-3B OCR model)
qwen-vl-utils>=0.0.8