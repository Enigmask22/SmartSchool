# Requirements for Render deployment with pre-built wheels
# Usage: pip install -r requirements-wheels.txt --find-links wheels

# =============================================================================
# WHEEL INSTALLATION INSTRUCTIONS:
# 1. Install from wheels first (for packages with build issues)
# 2. Install remaining packages from PyPI
# =============================================================================

# PROBLEMATIC PACKAGES (install from PyPI - wheels not Linux compatible)
# Fallback to PyPI installation since Windows wheels don't work on Render Linux
opencv-python-headless==4.8.1.78
insightface==0.7.3
onnxruntime>=1.17.0
onnx>=1.15.0
scikit-learn==1.3.2
numpy>=1.21.0
pandas>=2.0.0
python-jose[cryptography]==3.3.0
bcrypt==4.1.2
Pillow==10.0.1

# SAFE PACKAGES (install from PyPI)
fastapi==0.104.1
uvicorn[standard]==0.24.0
passlib[bcrypt]==1.7.4
supabase==2.15.3
python-dotenv==1.0.0
python-multipart==0.0.6
requests==2.31.0
aiofiles==23.2.1
email-validator==2.1.0
python-dateutil==2.8.2
typing-extensions>=4.12.2
websockets==12.0
google-generativeai==0.8.3
schedule==1.2.0
setuptools==68.2.2
wheel==0.41.2
